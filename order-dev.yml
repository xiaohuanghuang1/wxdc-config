server:
  port: 10002
#  application:
#    name: order
mybatis:
  mapper-locations: "classpath*:mapper/*Mapper.xml"

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: basic


